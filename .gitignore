# secrets / local state — never commit
.env
*.env
!.env.example
config/panels.local.yaml
secrets/
keys/
*.key
*_ed25519
*_ed25519.pub
.rbw/
vaultwarden.env
dev-vault.json
kiosk-host/host/secret-dir/
sealed-secret/
sealed-secrets/
*.sealed
secret-dir/

# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# dev runtime
dev/run/
*.log
.xephyr.pid

# build / packaging artifacts
dist/
*.deb
*.rpm
*.apk

# editor / agent-local automation (not part of the product)
.claude/
