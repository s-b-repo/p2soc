# Focused test: a single Chromium-engine panel to verify the CDP auto-login path.
display:
  auto: false
  width: 1280
  height: 800
  cols: 1
  rows: 1
  gap: 0

panels:
  - id: p3
    engine: chromium
    grid: [0, 0]
    mode: direct
    url: "http://127.0.0.1:9003/login"
    vault_item: "SOC Dev Panel 3"
    selectors: { user: "#username", pass: "#password", submit: "button[type=submit]" }
    login_marker: "#password"
    keepalive: { strategy: none }

tunnel: { enabled: false }
