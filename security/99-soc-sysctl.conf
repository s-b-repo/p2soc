# Installed to /etc/sysctl.d/99-soc.conf — tune the VM for zram on a 1 GB Pi.
# Favour compressed swap over reclaim thrash; small page-cluster suits zram.
vm.swappiness = 180
vm.watermark_boost_factor = 0
vm.watermark_scale_factor = 125
vm.page-cluster = 0
