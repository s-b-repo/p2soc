# Drop-in for getty@tty1.service — auto-login the kiosk user on tty1.
# Installed to /etc/systemd/system/getty@tty1.service.d/override.conf
# The kiosk user's ~/.bash_profile then `exec startx` -> Openbox -> the wall.
[Service]
ExecStart=
ExecStart=-/sbin/agetty --autologin soc --noclear %I $TERM
